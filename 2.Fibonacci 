2.Write a C/C++ program to generate the Fibonacci series:

a)Iteration 
#include <stdio.h>

int main() {
    int n, a = 0, b = 1, c, i;
    scanf("%d", &n);
    
    printf("%d %d ", a, b); // first two numbers

    for(i = 2; i < n; i++) {
        c = a + b;
        printf("%d ", c);
        a = b;
        b = c;
    }
    return 0;
}

   b) Using recursion

#include <stdio.h>

int fibonacci (int n) {
    if (n == 0 || n == 1)
        return n;
    else
        return fibonacci(n-2)+fibonacci(n -1);
}

int main() {
    int n;
    scanf("%d", &n);
    printf("%d", fibonacci (n));
    return 0;
}