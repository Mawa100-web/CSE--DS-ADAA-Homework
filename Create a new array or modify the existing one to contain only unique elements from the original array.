Create a new array or modify the existing one to contain only unique elements from the original array.


#include <stdio.h>

int main() {
    int arr[100], unique[100];
    int n, k = 0;

    printf("Enter number of elements: ");
    scanf("%d", &n);

    printf("Enter elements:\n");
    for(int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    for(int i = 0; i < n; i++) {
        int found = 0;
        for(int j = 0; j < k; j++) {
            if(arr[i] == unique[j]) {
                found = 1;
                break;
            }
        }
        if(!found) {
            unique[k] = arr[i];
            k++;
        }
    }

    printf("Unique elements:\n");
    for(int i = 0; i < k; i++) {
        printf("%d ", unique[i]);
    }

    return 0;
}