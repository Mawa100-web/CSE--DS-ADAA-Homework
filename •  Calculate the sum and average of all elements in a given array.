•  Calculate the sum and average of all elements in a given array.


#include <stdio.h>

int main() {
    int arr[5] = {10, 20, 30, 40, 50};
    int sum = 0;

    
    for(int i = 0; i < 5; i++) {
        sum = sum + arr[i];
    }

    float avg = sum / 5.0; 

    printf("Sum = %d\n", sum);
    printf("Average = %.2f\n", avg);

    return 0;
}
