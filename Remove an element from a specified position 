Remove an element from a specified position in an array.(Beginning, middle and end))





#include <stdio.h>

int main() {
    int a[100], n, pos;

    printf("Enter size: ");
    scanf("%d", &n);

    printf("Enter elements: ");
    for(int i = 0; i < n; i++)
        scanf("%d", &a[i]);

    printf("Enter position to delete: ");
    scanf("%d", &pos);

    if(pos < 0 || pos >= n) {
        printf("Invalid position");
        return 0;
    }

    for(int i = pos; i < n-1; i++)
        a[i] = a[i+1];

    n--; 

    printf("Array after deletion: ");
    for(int i = 0; i < n; i++)
        printf("%d ", a[i]);

    return 0;
}