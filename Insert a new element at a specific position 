Insert a new element at a specific position within an array.(Beginning, middle and end)



#include <stdio.h>

int main() {
    int arr[100] = {10, 20, 30, 40};
    int n = 4;
    int item, pos;

    
    item = 5;
    for (int i = n-1; i >= 0; i--)
        arr[i+1] = arr[i];
    arr[0] = item;
    n++;

    
    item = 99;
    pos = 2;
    for (int i = n-1; i >= pos; i--)
        arr[i+1] = arr[i];
    arr[pos] = item;
    n++;

    
    item = 55;
    arr[n] = item;
    n++;

    
    for (int i = 0; i < n; i++)
        printf("%d ", arr[i]);

    return 0;
}
